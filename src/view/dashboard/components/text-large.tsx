import styled from '@emotion/styled';
import { designToken } from '~core';

const StyledTextLarge = styled.div({
    fontSize: '1.1rem',
    fontWeight: 700,
    lineHeight: 1.5,
    color: designToken['gray-400'],
});

export default StyledTextLarge;
