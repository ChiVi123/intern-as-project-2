import styled from '@emotion/styled';

const StyledTextSmall = styled.div({
    fontSize: '0.9rem',
    fontWeight: 600,
    lineHeight: 1.5,
    color: 'inherit',
});

export default StyledTextSmall;
